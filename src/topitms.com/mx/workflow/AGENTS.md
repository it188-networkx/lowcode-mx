# Workflow 目录说明

本目录集中存放用于自动化业务流转和规则触发的工作流 (Workflow) JSON 配置文件。

## 内容特性
由于工作流具有定制化高、更新频繁的特点，在此按环境和具体业务需求，以 `.json` 文件形式持久化管理。它通常包含了：
- `Incident` 事件相关分配和响应。
- 邮件通知动作 (`sendemail`)。
- `ITOM` 警报同步与恢复机制。
- 参与者、地点和库存变动同步等功能逻辑流。